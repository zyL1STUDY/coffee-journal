# 11 待确认事项

## 设计源状态

| 问题 | 影响 | 状态 |
|---|---|---|
| Figma `03 Final Screens` 已确认存在 9 个最终 MVP 画面。 | 可以作为静态 UI 开发依据。 | 已确认 |
| 后续开发读取 Figma 时，应优先使用 `03 Final Screens` 或具体画面的 selection link，而不是 Cover 链接。 | 避免误读为只有封面。 | 已确认 |

## 页面与交互

| 问题 | 状态 |
|---|---|
| Splash 是否属于 MVP？ | TODO |
| Settings 是否属于 MVP 独立页面？ | TODO |
| 删除 Coffee Memory 是否需要确认弹窗？ | 已确认：必须二次确认。 |
| 编辑 Coffee Memory 的页面或流程 | 已确认：编辑保存后返回 Coffee Memory 并刷新内容；具体编辑 UI 可沿用记录表单模式。 |
| Record Flow 返回 / 取消规则 | 需补充细节：返回上一步或关闭 Bottom Sheet 的具体手势与确认。 |
| Photo Source 权限失败状态 | 已补充规则：显示错误提示，并允许返回或跳过照片。 |

## 业务逻辑

| 问题 | 状态 |
|---|---|
| 图片是否必填？ | 已确认：`photo_url` 可为空。 |
| 饮品名是否必填？ | 已确认：`drink_name` 可为空。 |
| source_type / source_name 是否必填？ | 已确认：`source_type` 和 `source_name` 必填。 |
| 保存后 AI Message 失败时是否仍保存记录？ | 已确认：AI 失败不影响保存 Coffee Record。 |
| 统计口径：本月咖啡、去过店铺、连续记录的计算规则。 | 已有基础规则；精确算法可在开发实现时细化。 |

## 数据与技术

| 问题 | 状态 |
|---|---|
| MVP 是否接入 Supabase？ | TODO |
| MVP 是否需要登录？ | TODO |
| 图片存储使用本地还是云端？ | TODO |
| AI Provider 和 Prompt 管理方式。 | TODO |
| 抠图 / Sticker 是 MVP 实现还是后续能力？ | 已建议：MVP 可先模拟，真正抠图作为后续能力。 |

## UX 风险

| 问题 | 状态 |
|---|---|
| Empty State 视觉稿未完整设计。 | 规则已补充，视觉可后续细化。 |
| Loading State 视觉稿未完整设计。 | 规则已补充，视觉可后续细化。 |
| Error State 视觉稿未完整设计。 | 规则已补充，视觉可后续细化。 |
| Toast / Success Feedback 视觉稿未完整设计。 | Coming Soon 和错误提示规则已补充，Toast 样式可后续细化。 |
